// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:ikXM4HV3GHGuHkQRJzD7@containers-us-west-12.railway.app:7240/railway"
}

generator client {
  provider = "prisma-client-js"
}

model Post {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
  text      String?
  userId    String?
  User      User?    @relation(fields: [userId], references: [id])
}

model User {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
  login     String   @unique
  password  String
  Post      Post[]
}
